# Agent Operating Rules

本仓库让 Agent 按固定广告优化方法论工作，而不是自由发挥。执行任何分析、报告、实验或账户建议时，先遵守这些规则。

## Default Objective

默认 ROI 是最终目标。注册、试用、关注、加购等都是过程指标，具体过程指标应随业务模型调整。

SaaS / B2B 默认漏斗：

```text
广告花费 -> 展示 -> 点击 -> 注册 -> 试用 -> 付费 -> 付费金额 -> ROI
```

分析时不得只看单个 CPA。必须说明 ROI 是否变差，以及问题落在哪个漏斗段。

## Required Funnel Checks

每次效果诊断都要检查：

- CPC 是否上升。
- 点击到注册率是否下降。
- 注册 CPA 是否异常。
- 注册到试用率是否下降。
- 试用 CPA 是否异常。
- 试用到付费率是否下降。
- 付费 CPA 是否异常。
- 客单价或付费金额是否下降。
- ROI 是否恶化。

如果某个环节数据缺失，要明确写出“缺失导致无法判断什么”，不能跳过。

## Controllable vs Business-Side

输出必须区分：

- 广告侧可执行：预算、出价、账户结构、关键词、搜索词、受众、素材、地域、设备、排期、落地页路由、实验。
- 落地页/网站侧建议：页面匹配度、速度、表单、价格展示、CTA、试用路径。
- 产品侧建议：功能供给、套餐、试用门槛、激活路径、付费墙。
- 销售/运营侧建议：线索跟进、试用激活、邮件/CRM、人工触达。
- 需要业务决策：ROI 目标、增长与利润权衡、市场取舍、供给扩展、定价和预算上限。

当业务相关场景基本都尝试并淘汰时，不要继续把结论停在“优化账户结构”。要判断是否新增有效供给不足，并给出证据边界和下一步实验。

## Multi-Goal Pressure Rule

当 ROI、注册、试用多个目标同时压制，且账户付费能力持续削弱时，默认判断路径是：

1. 高 ROI 要求会压缩流量池。
2. 流量池压缩会减少新数据和付费样本。
3. 样本减少会削弱学习能力，账户可能进入保守收缩状态。
4. 此时不要只建议“继续优化结构”；必须提出广告侧可验证实验和业务侧供给判断。

## Evidence Rules

- 结论必须绑定数据证据、时间范围和对比基线。
- 区分事实、假设和建议。
- 不把平台归因直接当作因果证明。
- 不在授权外执行账户写操作。
- 不输出或提交凭据、token、客户数据、个人数据、受众名单或未脱敏报表。

## Skill Writing Rules

新的 skill 必须包含：适用场景、输入数据要求、分析顺序、必须拆解的指标、必须分层的维度、判断阈值、不允许跳过的证据、输出格式和动作分类。
