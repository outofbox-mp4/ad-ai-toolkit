# Contributing

欢迎提交新的 methodology、SOP、skill、模板、工具条目与改进建议。这个仓库不是 prompt 集合，新增内容必须服务真实广告优化工作，并能让 Agent 稳定复用同一套判断逻辑。

## 方法论资产要求

- 分析类内容默认 ROI 是最终目标，注册、试用等为过程指标。
- 必须能拆完整漏斗：广告花费 -> 展示 -> 点击 -> 注册 -> 试用 -> 付费 -> 付费金额 -> ROI。
- 必须区分广告侧可控问题和业务侧问题。
- 不要把“继续优化账户结构”作为没有证据的兜底结论。
- 示例只能使用虚构或已脱敏数据。

## 提交 Skill

每个 skill 放在 `skills/<skill-name>/`，并至少包含 `SKILL.md`：

- 使用小写连字符名称，聚焦一个可识别的广告工作流。
- frontmatter 只包含 `name` 与明确的 `description`。
- 必须包含适用场景、输入数据要求、分析顺序、必须拆解的指标、必须分层的维度、判断阈值、不允许跳过的证据、输出格式、动作分类和安全边界。
- 不把未验证的策略、平台规则或产品声明写成事实。
- 必要时附带可公开、可复现的脱敏样例。

## 提交工具条目

在 `tools/README.md` 的模板基础上补齐信息。请避免联盟链接、无证据的排名和营销文案。

## Pull Request 检查表

- [ ] 不包含密钥、令牌、客户数据或个人数据
- [ ] 链接、定价与平台能力已注明验证日期
- [ ] 新 skill 已通过结构校验
- [ ] 变更说明了适用范围、风险与回滚方式（如适用）
